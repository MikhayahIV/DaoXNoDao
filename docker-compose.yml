version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: postgres_designer_patterns_faculdade
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5434:5432"
    restart: always